<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .report-section {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #dee2e6;
            border-radius: 5px;
        }
        .stat-item {
            margin: 10px 0;
        }
        .footer {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>{{subject}}</h2>
            <p>Dear {{adminName}},</p>
            <p>Here is your daily report for {{date}}:</p>
        </div>

        {{#if reportData.totalTasks}}
        <div class="report-section">
            <h3>Task Completion Rate Report</h3>
            <div class="stat-item">Total Tasks: {{reportData.totalTasks}}</div>
            <div class="stat-item">Completed Tasks: {{reportData.completedTasks}}</div>
            <div class="stat-item">Completion Rate: {{reportData.completionRate}}%</div>
            {{#if reportData.statusBreakdown}}
            <h4>Status Breakdown:</h4>
            {{#each reportData.statusBreakdown}}
            <div class="stat-item">{{status}}: {{count}}</div>
            {{/each}}
            {{/if}}
        </div>
        {{/if}}

        {{#if reportData.tasksCompleted}}
        <div class="report-section">
            <h3>Daily Summary</h3>
            <div class="stat-item">Tasks Completed Today: {{reportData.tasksCompleted}}</div>
            <div class="stat-item">Tasks Created Today: {{reportData.tasksCreated}}</div>
            <div class="stat-item">Projects Completed Today: {{reportData.projectsCompleted}}</div>
            <div class="stat-item">Projects Created Today: {{reportData.projectsCreated}}</div>
            <div class="stat-item">Active Users Today: {{reportData.activeUsers}}</div>
        </div>
        {{/if}}

        <div class="footer">
            <p>This is an automated report generated by the Task Management System.</p>
        </div>
    </div>
</body>
</html> 