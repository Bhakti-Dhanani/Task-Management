<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .section {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        .table th, .table td {
            padding: 8px;
            border: 1px solid #ddd;
            text-align: left;
        }
        .table th {
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Daily Task Management Summary</h2>
            <p>Hello {{adminName}},</p>
            <p>Here's your daily summary for {{date}}:</p>
        </div>

        <div class="section">
            <h3>Yesterday's Overview</h3>
            <ul>
                <li>New Tasks Created: {{newTasks}}</li>
                <li>Tasks Completed: {{completedTasks}}</li>
                <li>New Projects Created: {{newProjects}}</li>
            </ul>
        </div>

        <div class="section">
            <h3>Task Completion Rates by User</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>User</th>
                        <th>Total Tasks</th>
                        <th>Completed</th>
                        <th>Completion Rate</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each completionRates}}
                    <tr>
                        <td>{{userName}}</td>
                        <td>{{totalTasks}}</td>
                        <td>{{completedTasks}}</td>
                        <td>{{completionRate}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="section">
            <h3>Pending Tasks by Project</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>Project</th>
                        <th>Pending Tasks</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each pendingTasks}}
                    <tr>
                        <td>{{projectTitle}}</td>
                        <td>{{pendingTasksCount}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="section">
            <p>This is an automated report generated by the Task Management System.</p>
        </div>
    </div>
</body>
</html> 